@AGENTS.md

Use the repo rules in `AGENTS.md` as the source of truth.

For consistency cleanup, first read:
- `.cursor/skills/suggest-app-consistency/SKILL.md`
- `docs/DESIGN_SYSTEM.md`
- `docs/APP_DESIGN_SPEC.md`

When touching UI, keep all user-facing strings in `src/i18n/messages/{ja,en,ko}.json` and update all three locales together.
